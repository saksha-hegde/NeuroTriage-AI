# components/worklist/

`WorklistTable` (rows navigate to the Reading Experience on click),
`StatusBadge` (`StudyStatusBadge` + `AIStatusBadge`), `PriorityBadge`
(matches the UX doc's 🔴🟠🟡🟢 policy table), `SimulateStudyButton`,
`ResetDemoButton` ("Reset Demo" - the confirmation step lives in
`WorklistPage` via `components/common/ConfirmDialog`, not in this button
itself).
