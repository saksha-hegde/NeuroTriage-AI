# components/common/

`ConfirmDialog` — small centered modal for confirming a destructive/hard-to-
reverse action (currently just "Reset Demo", see `worklist/ResetDemoButton`).
Reusable for future confirmation steps rather than a one-off inline in
`WorklistPage`.
