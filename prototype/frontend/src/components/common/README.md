# components/common/

Empty so far — nothing has needed extracting out of `worklist/`/`reading/`
yet. Candidate for Milestone 8 polish if shared pieces emerge (e.g. a toast/
banner component for the planned "AI unavailable" fallback state).
